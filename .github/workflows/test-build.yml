name: releaser

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  create_release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: Build
        shell: bash
        run: |
          bash -x build.bash
